// 
// Why OOP in MS is evil
// Vol. 1

CommandBase = {} // Let it be a base class for Marinette's commands
CommandBase.conditions = [] // A list of conditions before making a command happen

ImplHelp = new CommandBase // Implementing, for example, help command
ImplHelp.conditions.push(someAbstractCondition) // Adding condition to it

print(ImplHelp.conditions == CommandBase.conditions) // This is true now! Because .conditions property is static BY DEFAULT!
